{
  "_comment": "LEGACY FILE - This IAM policy is no longer used. IAM roles are now managed by CDK (see cdk/stacks/batch_java_processor_stack.py). Kept for reference only.",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "s3:PutObject",
        "s3:PutObjectAcl",
        "s3:GetObject",
        "s3:ListBucket"
      ],
      "Resource": [
        "arn:aws:s3:::your-output-bucket/*",
        "arn:aws:s3:::your-output-bucket"
      ]
    }
  ]
}
